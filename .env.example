# Database
DATABASE_URL="postgresql://user:password@localhost:5432/splstats?schema=public"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-here-generate-with-openssl-rand-base64-32"

# GitHub OAuth (create at https://github.com/settings/developers)
GITHUB_ID="your_github_client_id"
GITHUB_SECRET="your_github_client_secret"
GITHUB_ALLOWED_USERS="githubusername1,githubusername2"

# Encryption Key for storing tokens (generate with: openssl rand -base64 32)
ENCRYPTION_KEY="your-encryption-key-here-32-bytes"

# Logging
# Set to "true" to echo log messages to the console (stdout).
# Recommended: true for local development, false (or unset) in production.
# Logs are always written to the DB regardless of this setting.
LOG_CONSOLE="true"
